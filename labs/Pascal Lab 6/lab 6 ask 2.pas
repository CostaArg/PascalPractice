Program tetragono_ak;

Var
	square, number: Integer;

Begin

	Write ('Enter a number              : ');
	Read (number);
	square:= (number * number);
	Write ('The square of the number is : ', square);

End.